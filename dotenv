# app information
APP_NAME = "Rolling"

# logging
LOG_LEVEL = "INFO"

# database connection
DATABASE_URL="postgres://<DB_USER>:<DB_PASS>@<DB_HOST>:<DB_PORT>/rolling" # production user

DB_HOST=""
DB_NAME="rolling"
DB_USER=""
DB_PASS=""
DB_PORT=""

# database pool connection
DB_SSLMODE = "prefer" # controls how postgres client negootaites tls/ssl encryption 
DB_POOL_MIN_SIZE = 2
DB_POOL_MAX_SIZE = 10
DB_POOL_TIMEOUT = 10
DB_POOL_MAX_IDLE = 300
DB_POOL_MAX_LIFETIME = 3600
DB_POOL_NUM_WORKERS = 3